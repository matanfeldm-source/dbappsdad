name: customer-journey-app
version: 1.0.0
description: Interactive customer journey visualization app for home appliance clients

# Runtime configuration
runtime:
  type: python
  version: "3.9"

# Application configuration
app:
  entrypoint: ./entrypoint.sh
  port: 3000
  environment:
    PYTHONUNBUFFERED: "1"
    PORT: "3000"

# Resources
resources:
  requirements: ./backend_python/requirements.txt

# Build configuration (if needed)
# build:
#   commands:
#     - cd frontend && npm install && npm run build

